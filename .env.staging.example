# =============================================================
# StockHub Backend — Variables d'environnement Staging
# =============================================================
# Copier ce fichier et remplir les valeurs dans le dashboard Render
# NE JAMAIS committer les valeurs réelles
# =============================================================

NODE_ENV=staging

# --- Base de données Aiven MySQL (free tier) ---
# ⚠️  L'instance Aiven free tier expire après ~30 jours d'inactivité.
#     Si la connexion échoue, recréer une instance dans Aiven Console
#     et mettre à jour DATABASE_URL + les variables DB_* ci-dessous.
DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DATABASE?ssl-mode=REQUIRED

DB_HOST=your-aiven-mysql-host.aivencloud.com
DB_USER=avnadmin
DB_PASSWORD=your-aiven-password
DB_PORT=3306
DB_DATABASE=defaultdb
DB_SSL=true
DB_CONNECTION_LIMIT=3
DB_MAX_IDLE=3

# --- Azure AD B2C ---
CLIENT_ID=your-azure-ad-b2c-client-id
CLIENT_SECRET=your-azure-ad-b2c-client-secret
AZURE_USE_ROPC_POLICY=false
AZURE_ROPC_CLIENT_ID=your-ropc-client-id

# --- CORS ---
ALLOWED_ORIGINS=https://stock-hub-v2-front.vercel.app,https://brave-field-03611eb03.5.azurestaticapps.net
VERCEL_PREVIEW_CORS=true

# --- Port ---
PORT=3006

# --- Seed ---
# Email de l'utilisateur owner créé par prisma/seed.ts
SEED_OWNER_EMAIL=your-owner-email@example.com
